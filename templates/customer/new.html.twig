{# templates/customer/new.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}New Customer{% endblock %}

{% block body %}
    <h1>Create New Customer</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('customer_list') }}">Back to list</a>
{% endblock %}
